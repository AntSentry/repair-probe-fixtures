# repair-probe-fixtures

A tiny node:test fixture used to probe the Kodusius repair-runner end to end
(2026-09-06). All content was authored for the probe; it is not part of any
benchmark corpus. Branches `probe/defect-*` carry a one-line seeded defect
in `src/counter.js` at their heads.
